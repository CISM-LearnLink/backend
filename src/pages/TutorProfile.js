import React from 'react';
// TODO: Fetch tutor data by ID and display profile, allow booking if user is parent

function TutorProfile({ tutorId, user }) {
  return (
    <div>
      <h2>Tutor Profile (to be implemented)</h2>
      <p>Tutor ID: {tutorId}</p>
      {/* Display tutor info, subjects, bio, hourly rate, booking button, etc. */}
    </div>
  );
}

export default TutorProfile; 