import React from 'react';
// TODO: Fetch recommended tutors, search tutors, display bookings

function ParentDashboard({ user }) {
  return (
    <div>
      <h2>Parent Dashboard (to be implemented)</h2>
      {/* Display recommended tutors, search/filter, bookings, etc. */}
    </div>
  );
}

export default ParentDashboard; 